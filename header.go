package rest

const (
	// HeaderContentType is the standard content type header
	HeaderContentType = `Content-Type`
)

const (
	// ContentJSON is the standard json content type
	ContentJSON = `application/json`

	// ContentYAML is the standard yaml content type
	ContentYAML = `application/yaml`

	// ContentXML is the standard yaml content type
	ContentXML = `application/xml`
)
